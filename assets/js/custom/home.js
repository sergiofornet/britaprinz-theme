( function() {
	// let homeSlider = document.querySelector('.home__slider');
	// if (homeSlider) {
	// 	const slider = new Slider(homeSlider);
	// 	console.log(slider);
	// 	async function autoplay() {
	// 		await wait(4000);
	// 		slider.move();
	// 		requestAnimationFrame(autoplay);
	// 	}
	// 	requestAnimationFrame(autoplay);
	// }
}() );
